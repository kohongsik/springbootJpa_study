# test / resource 에 더 우선권을 가짐.
# default 를 인메모리 라서 안써도 인메모리로 가져옴 주석해도 괜찮음
spring:
#  datasource:
#    url: jdbc:h2:mem:test #test 환경에서 in memory로 db 뛰움 / h2가 자바로 만든거라서 jvm안에서 띄울 수 있음.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug # logger 에서 출력
    org.hibernate.type: trace # parameter(? 로 표시) 부분을 확인 할 수 있음